{
  /* Use the config that doesn't have the aliases for rollup. */
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "dist",
  },
  "include": [
    "./src/**/*"
  ]
}
